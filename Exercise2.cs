using System;

namespace patika_algorithm_exercises
{
    class Exercise2
    {
        /* static void Main(string[] args)
        {
            //? In a console application, ask the user to enter two positive numbers (n, m).
            //? Then ask the user to enter n positive numbers.
            //? From the numbers entered by the user, print the numbers that are divisible by m to the console.
            
            Console.WriteLine("Please enter 2 positive number");
            Console.Write("Enter n: ");
            int n = Convert.ToInt32(Console.ReadLine());
            Console.Write("Enter m: ");
            int m = Convert.ToInt32(Console.ReadLine());
            
            Console.WriteLine("Please enter {0} number:", n);

            for (int i = 0; i < n; i++)
            {
                Console.WriteLine("Enter {0}. number: ", i+1);
                int number = Convert.ToInt32(Console.ReadLine());
                if((number==m) || (number % m == 0))
                {
                    Console.WriteLine(number+" is divisible with m");
                }
                else
                {
                    Console.WriteLine(number+" is not divisible with m");
                }
            }

        } */
    }
}