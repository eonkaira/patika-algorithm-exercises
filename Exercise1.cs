using System;

namespace patika_algorithm_exercises
{
    class Exercise1
    {
        /* static void Main(string[] args)
        {

            //? In a console application, ask the user to enter a positive number, then ask the user to enter n positive numbers.
            //? Print the even numbers that the user has entered to the console.

            Console.Write("Please enter a positive number:");
            int n= Convert.ToInt32(Console.ReadLine());
            Console.WriteLine("Please enter {0} positive number:", n);

            for (int i = 0; i < n; i++)
            {
                Console.WriteLine("Enter {0}. number: ", i+1);
                int number=Convert.ToInt32(Console.ReadLine());

                if(number%2==0)
                {
                    Console.WriteLine(number+" is an even number.");
                }
                else
                {
                    Console.WriteLine(number+" is not an even number.");
                }
            }

           
        } */
    }
}